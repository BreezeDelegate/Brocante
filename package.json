{
  "name": "brocante",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@11",
  "engines": {
    "node": ">=24 <25"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @brocante/web & npm run dev -w @brocante/api",
    "build": "npm run build -w @brocante/web && npm run build -w @brocante/api",
    "test": "npm run test -w @brocante/web && npm run test -w @brocante/api",
    "typecheck": "npm run typecheck -w @brocante/web && npm run typecheck -w @brocante/api",
    "lint": "oxlint apps --deny-warnings",
    "format": "prettier --write .",
    "format:check": "prettier apps/web/src/api.ts && exit 1",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "oxlint": "^1.76.0",
    "prettier": "^3.9.6"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "fsevents@2.3.2": true,
    "fsevents@2.3.3": true
  }
}
